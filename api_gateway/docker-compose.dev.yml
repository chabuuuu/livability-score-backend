version: "3.9"
services:
  nginx-api-gateway-kltn-be:
    image: nginx:alpine
    container_name: nginx-api-gateway-kltn-be
    ports:
      - "9000:80"
    volumes:
      - ./nginx/dev:/etc/nginx/conf.d
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
